const RULE_VARIABLE_TYPES = Object.freeze({
   BOOLEAN: "boolean",
   NUMBER: "number",
   STRING: "string"
})

const RULE_VARIABLE_TYPE_VALUES = Object.values(RULE_VARIABLE_TYPES)

module.exports = {
   RULE_VARIABLE_TYPES,
   RULE_VARIABLE_TYPE_VALUES
}
